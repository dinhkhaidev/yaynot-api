# Nixpacks configuration for Railway
# Railway uses Nixpacks to build Node.js apps

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[start]
# Railway will use Procfile if exists, otherwise this
cmd = "node server.js"

[variables]
NODE_ENV = "production"
